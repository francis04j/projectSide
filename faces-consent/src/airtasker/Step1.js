import { Button, TextField, Container, Typography } from '@mui/material';

const Step1 = () => (
  <Container>
    <Typography variant="h5">Step 1: Task Details</Typography>
    <TextField label="What do you need done?" fullWidth margin="normal" />
  </Container>
);
